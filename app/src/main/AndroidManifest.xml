<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ramim.ramim.javaBook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".AboutActivity"></activity>
        <activity android:name=".QuestionActivity" />
        <activity android:name=".QuizeActivity" />
        <activity android:name=".EncapculationActivity" />
        <activity android:name=".InterfaceActivity" />
        <activity android:name=".AbstractionActivity" />
        <activity android:name=".ConstarActivity" />
        <activity android:name=".PolyActivity" />
        <activity android:name=".objectActivity" />
        <activity android:name=".Main2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".signUpACtivity" />
        <activity android:name=".SignInActivity" />
    </application>

</manifest>